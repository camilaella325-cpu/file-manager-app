name: file_manager_app
description: A cross-platform file manager with dark mode, SD card support, and storage analysis.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Theme
  cupertino_icons: ^1.0.2
  google_fonts: ^6.0.0
  
  # File & Storage
  file_picker: ^6.0.0
  path_provider: ^2.1.1
  permission_handler: ^11.4.3
  
  # Storage Analysis
  disk_space: ^1.2.5
  
  # State Management
  provider: ^6.0.0
  
  # Utils
  intl: ^0.19.0
  
  # URL Launcher - for opening social media links
  url_launcher: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
